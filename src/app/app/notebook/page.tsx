import { AppShell } from '@/components/layout';
import { NotebookEditor } from '@/components/editor';

export default function NotebookPage() {
  return (
    <AppShell>
      <NotebookEditor />
    </AppShell>
  );
}
